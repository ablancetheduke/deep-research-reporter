{
  "mcpServers": {
    "deep-research-reporter": {
      "command": "python",
      "args": ["-m", "drr.cli", "--provider", "mcp"],
      "env": {
        "MCP_SERVER_URL": "http://localhost:3000",
        "MCP_API_KEY": "your-api-key-here"
      }
    }
  }
}
